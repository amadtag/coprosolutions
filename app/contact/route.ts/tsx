export default function ContactPage() {
  return (
    <main style={{ padding: 20, maxWidth: 600 }}>
      <h1>Demande de devis</h1>

      <p>
        Remplissez le formulaire ci-dessous pour recevoir un devis Coprosolutions.
      </p>

      <form
        style={{ display: "flex", flexDirection: "column", gap: 10 }}
      >

        <input
          type="text"
          placeholder="Nom de la copropriété"
          required
          style={{ padding: 10 }}
        />

        <input
          type="text"
          placeholder="Nom du contact"
          required
          style={{ padding: 10 }}
        />

        <input
          type="email"
          placeholder="Email"
          required
          style={{ padding: 10 }}
        />

        <input
          type="tel"
          placeholder="Téléphone"
          style={{ padding: 10 }}
        />

        <select style={{ padding: 10 }}>
          <option>Type de service</option>
          <option>Entretien copropriété</option>
          <option>Remplacement de gardien</option>
          <option>Nettoyage exceptionnel</option>
          <option>Mise en relation gardien</option>
        </select>

        <textarea
          placeholder="Votre demande"
          rows={5}
          style={{ padding: 10 }}
        />

        <button
          type="submit"
          style={{
            padding: 12,
            background: "#0f172a",
            color: "white",
            border: "none",
            cursor: "pointer"
          }}
        >
          Envoyer la demande
        </button>

      </form>
    </main>
  );
}